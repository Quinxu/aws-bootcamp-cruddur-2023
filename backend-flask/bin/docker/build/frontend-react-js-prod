#! /usr/bin/bash
ABS_PATH=$(readlink -f "$0")
BUILD_PATH=$(dirname $ABS_PATH)
DOCKER_PATH=$(dirname $BUILD_PATH)
BIN_PATH=$(dirname $DOCKER_PATH)
BACKEND_FLASK_PATH=$(dirname $BIN_PATH)
PROJECT_PATH=$(dirname $BACKEND_FLASK_PATH)

echo $ABS_PATH
echo $BUILD_PATH
echo $DOCKER_PATH
echo $BIN_PATH
echo $BACKEND_FLASK_PATH
echo $PROJECT_PATH

# docker build \
# --build-arg REACT_APP_BACKEND_URL="https://api.qinxu.shop" \
# --build-arg REACT_APP_AWS_PROJECT_REGION="$AWS_DEFAULT_REGION" \
# --build-arg REACT_APP_AWS_COGNITO_REGION="$AWS_DEFAULT_REGION" \
# --build-arg REACT_APP_AWS_USER_POOLS_ID="$AWS_COGNITO_USER_POOL_ID" \
# --build-arg REACT_APP_CLIENT_ID="2ar9lb34g0lomto8mi7h3neilf" \
# -t frontend-react-js \
# -f Dockerfile.prod \
# .