#! /usr/bin/bash

ABS_PATH=$(readlink -f "$0")
SERVERLESS_PATH=$(dirname $ABS_PATH)
IMAGE_FILE_PATH="$SERVERLESS_PATH/files/star-trek-data.jpg"

aws s3 cp "$IMAGE_FILE_PATH" "s3://assets.$DOMAIN_NAME/avatars/original/"